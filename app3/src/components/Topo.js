import React from 'react';
import { View, Image } from 'react-native';
import jokenpoPng from '../../img/jokenpo.png';

const Topo = () => (
    <View>
        <Image source={jokenpoPng} />
    </View>
);

export default Topo;
